<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body class="col-flexbox-container">
    <!-- <div>
        <p class="flexbox-item">Form submitted</p>
    </div> -->
    <p class="flexbox-item">Form submitted</p>
    <p>Payment Date: {{ currentDate }}</p>
    <div>
        <h2 class="flexbox-item">Payment Summary</h2>
    </div>
    
    <p>Sender First Name: {{ senderFirstName }}</p>
    <p>Sender Last Name: {{ senderLastName }}</p>
    <img src="{{senderImagePath}}" alt="sender-image" width="200px">
    
    <p>Recipient First Name: {{ senderFirstName }}</p>
    <p>Recipient Last Name: {{ senderLastName }}</p>
    <p>Message: {{ message }}</p>
    <p>Notification method: {{ notificationMethod }}</p>
    <p>Email: {{ email }}</p>
    <p>Phone: {{ phone }}</p>

    <h2>Payment Details</h2>
    <p>Card Type: {{ cardType }}</p>
    <p>Card Expiration Date: {{ expirationDate }}</p>
    <p>Amount: {{ amount }}</p>
</body>
</html>